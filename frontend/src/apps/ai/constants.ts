// Shared UI constants for the AI feature (region labeling used by both the
// chat page and the AI 模型配置 settings module).

export const REGION_COLOR: Record<string, string> = {
  global: "blue",
  china: "magenta",
  proxy: "purple",
};

export const REGION_LABEL: Record<string, string> = {
  global: "国际",
  china: "国内",
  proxy: "中转/聚合",
};
